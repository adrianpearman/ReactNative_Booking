import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  // Content Containers
  mainContainer: {
    paddingTop: 70,
    flex: 0.5
  },
  loadingContainer: {
    paddingTop: 60,
    flex: 1,
    alignContent: "center"
  }
});

export { styles };
